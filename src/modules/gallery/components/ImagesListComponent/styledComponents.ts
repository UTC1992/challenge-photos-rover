import { ImageListItem, styled } from '@mui/material'

export const ImageBoxStyled = styled(ImageListItem)(() => ({
  border: '1px solid #8d9da0',
  borderRadius: '10px',
  boxShadow: '0 0 10px rgba(0, 0, 0, 0.1)',
}))
